/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuración preparada para el dominio oidaterapia.org
  images: {
    domains: ['oidaterapia.org'],
  },
};

export default nextConfig;
